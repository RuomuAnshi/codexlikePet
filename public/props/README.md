# SakiPet 内置道具

这些是随应用发布的轻量 SVG 道具，不需要用户额外下载。道具只包含图片和元数据，互动规则由 Rust 社交运行时控制。

- `football`：踢球、滚动、反弹、追逐、传球、抢球
- `snack`：发现、分享、吃掉、争抢
- `plush`：抱走、拉扯、传递、抢夺、安慰

图片为 SakiPet 内置原创简化素材，统一使用应用的粉蓝配色。每个道具的 `prop.json` 只声明碰撞体和允许的高层行为；AI 不能直接控制坐标、速度或窗口。
